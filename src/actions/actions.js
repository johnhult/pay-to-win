import decrement from 'actions/decrement';
import increment from 'actions/increment';

const BUTTON_INCREASE_AND_DECREASE = () => ({
	decrement,
	increment
});

export default BUTTON_INCREASE_AND_DECREASE;
